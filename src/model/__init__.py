"""Model packages for the Lyapunov-discriminator project.

`model.dit` holds the main vision transformer (LyapunovDiT) and its
auxiliary modules (text encoder, VAE wrapper, schedule, losses,
checkpoint adapters).
"""
